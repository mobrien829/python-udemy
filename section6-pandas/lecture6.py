# we can use the .groupby() and then a column header string as the argument
# to make a groupby object that we can perform functions on

# .nunique and .unique: nunique gives number of unique values, .unique gives the actual values

# .value_counts() returns a series of values and number of times they appeared

# we can use .apply(function name) to apply a function to a column

# ex:
# df['col1'].apply(times2) will double everything in column 2

